\documentclass[11pt]{article}

% arXiv-friendly: single-spaced, standard packages found in TeX Live.
% No minted, no -shell-escape, no externalized TikZ.
\usepackage[margin=1in]{geometry}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{array,longtable}
\usepackage{listings}
\usepackage{url,xcolor}
\usepackage[colorlinks=true,linkcolor=black,citecolor=blue,urlcolor=blue]{hyperref}

% ------------------------------------------------------------------
% Metadata (arXiv builds these from the submission form, not the tex)
% ------------------------------------------------------------------
\title{Adam Prism: A Sovereignty-First Architecture for Verified,\\
Deterministic LLM Agents}
\author{Mohamed Othman\\
  Independent Researcher, Cairo, Egypt\\
  \texttt{othmastar@github}}
\date{September 2026}

\newcommand{\passUScomment}[1]{}

\begin{document}
\maketitle

\begin{abstract}
Autonomous LLM agents that generate code, documents, or system artifacts face a
structural problem: their outputs are stochastic, while the environments they must
produce for are deterministic. Orchestration frameworks such as LangGraph, CrewAI, and
AutoGen move control out of the model loop, yet verification still relies on
LLM-as-a-judge --- a probabilistic system supervising a probabilistic system. We present
\textbf{Adam Prism}, a sovereignty-first agent architecture that treats verification as a
first-class architectural layer rather than an afterthought. Adam Prism separates
planning, execution, verification, and repair into independent stages: a
schema-validated plan drives sandboxed step execution; a deep-verification loop combines
deterministic functional checks (compilation, import, and test execution) with an
independent spec-conformance judge --- an LLM instantiated separately from the builder so
that a product is measured against the requested goal, not against its own tests; a
bounded escalation ladder repairs defects with an honest stop when convergence is
impossible. On top of this loop, a governance layer provides human-in-the-loop approval
gates, scheduled background jobs, durable agent teams, and rotating credential storage.
Because deterministic checks carry the heavy validation load, the system performs
1{,}317 automated tests without any LLM call, executes 26 scenario workflows, and
assembles hardened Linux distributions from a natural-language goal. Two independent
oracle-checked registries --- 10 CSV-verification and 5 code-generation scenarios
(executable hidden cases) --- reach \textbf{10/10} and \textbf{15/15 (100\%)}
single-shot. Everything runs fully locally on a 12B model via Ollama: zero cost, no
telemetry, air-gap capable. We report a failure-mode taxonomy, a system-level benchmark
of the two-role execution split, and an honest $\tau$-bench probe of raw model capability.
\end{abstract}

\noindent\textbf{Keywords:} agentic AI; multi-agent orchestration; verification;
sovereign AI; local LLM; deterministic evaluation; spec-conformance.

% ------------------------------------------------------------------
\section{Introduction}\label{sec:intro}
The shift from generative AI to agentic AI --- systems that act on external environments ---
exposes a fundamental architectural mismatch. Large language models produce stochastic,
unstructured tokens; the backends they control --- file systems, compilers, test runners,
package managers, operating-system builders --- require deterministic, schema-conformant
inputs. Every agent framework must therefore answer one question: \emph{who decides what
is ``done''?}

The dominant answer in 2026 is \emph{LLM-as-a-judge}: a model scores another model's
output. Recent research demonstrates why this is fragile. FORMALJUDGE
(\cite{formajudge}, arXiv:2602.11136) articulates the core dilemma --- ``how can
probabilistic systems
reliably supervise other probabilistic systems without inheriting their failure
modes?'' --- and proposes neuro-symbolic separation. AGENTLTL (\cite{agentltl},
arXiv:2607.02599) shows
that procedural compliance can be scored deterministically, judge-free, and that the same
constraints can gate execution online and drive training. Verified Multi-Agent
Orchestration (\cite{vmao}, arXiv:2603.11445) demonstrates that an orchestration-level
verifier,
separate from the executing model, improves answer completeness by more than a point on
1--5 rubrics. ToolBench-X (\cite{toolbenchx}, arXiv:2606.25819) shows that agents fail far
more because they
cannot \emph{diagnose} environment hazards than because they lack compute --- and that
targeted diagnosis recovers many previously-failed tasks where test-time scaling yields
limited gains. General AgentBench
(\cite{gentbench}, arXiv:2602.18998) identifies a persistent \emph{verification gap}:
generation capacity
consistently exceeds selection capacity, so test-time scale alone does not buy
correctness.

These results converge on a design principle we adopt directly: \textbf{decouple
verification from the system being verified, and make verification as deterministic as
the target environment permits.}

Independently, the multi-agent framework landscape measures its own gap. Independent
2026 benchmarks of LangGraph, CrewAI, and AutoGen agree on the shape of the trade-off:
LangGraph adds the lowest orchestration overhead ($\sim$9\% tokens) and the best latency,
but imposes a steep learning curve and manual fault recovery; CrewAI prototypes fastest
but carries $\sim$18\% token overhead and poor debugging; AutoGen's conversational model
scales cost poorly on chain depth and was placed in maintenance mode by its maintainers.
None of them treats \emph{repair} and \emph{independent verification} as native
architectural layers.

Adam Prism is a different bet: a \textbf{sovereignty-first, verification-driven agent}
that runs entirely on local hardware. Its contributions are:
\begin{enumerate}
  \item \textbf{A verification-driven execution model} (Plan
        $\rightarrow$ Execute $\rightarrow$ Verify $\rightarrow$ Repair
        $\rightarrow$ Gate) where the verifier is architecturally separated from the
        builder, and where an independent judge measures the product against the original
        goal.
  \item \textbf{Deterministic grounding of evaluation} --- compilation, import, test
        execution, and statistical validation (distribution-shape and deviation tests,
        duplicate detection) run without any model call, so regression suites are
        deterministic and reproducible.
  \item \textbf{An honest bounded-repair loop} --- defects are repaired through an
        escalation ladder with an explicit, documented stop condition when convergence is
        impossible, rather than an open-ended retry.
  \item \textbf{Reusable scenario and tree workflows} --- 26 goal-driven workflows that
        turn natural language into executed, verified artifacts.
  \item \textbf{Sovereign deployment} --- zero telemetry, air-gap native, single local
        12B model, no per-token cost.
\end{enumerate}

% ------------------------------------------------------------------
\section{Architecture Overview}\label{sec:arch}
\subsection{Design Principles}
\begin{itemize}
  \item \textbf{P1 --- Verify, don't assume.} The cost of trust must be paid with checks,
        not prompts. Whenever a requirement can be checked deterministically (parse,
        compile, import, run tests, statistical conformance), no LLM is consulted.
  \item \textbf{P2 --- The judge is not the builder.} Quality must be measured against the
        original goal, by a component instantiated separately from the module that
        produced the artifact. A self-written test suite can verify a drifted interface;
        the judge's contract is to catch that drift.
  \item \textbf{P3 --- Plans are real, validated objects.} Goals become structured plans
        (task trees or scenario steps) validated against a schema before execution.
        Control flow lives in the plan, not in an opaque reasoning loop.
  \item \textbf{P4 --- Honest failure beats fake success.} Every loop has a bounded,
        documented stopping rule; when repair cannot converge, the system reports the
        failure truthfully instead of returning a superficially plausible artifact.
  \item \textbf{P5 --- The machine belongs to the user.} Local-only execution by default;
        no analytics, no callbacks, no per-token billing.
\end{itemize}

\subsection{Block Diagram}
The block diagram below is intentionally implementation-agnostic (no code, no internal
module names, no filenames); it describes the control plane of the system.

\begin{lstlisting}[basicstyle=\ttfamily\footnotesize,frame=single,breaklines]
                USER (goal in natural language)
                           |
                           v
             [ 1. PLAN                ]   goal -> validated plan
             [    schema-gated, indexed]   (task tree / scenario)
                           |
                           v
             [ 2. EXECUTE             ] ---> [ SANDBOX (isolated env) ]
             [    batched/parallel    ]      [ files/tests/shells    ]
             [    gated tool dispatch ]
                           |
                           v
             [ 3. VERIFY              ]
             [    deterministic checks]
             [    independent judge   ]
             [    product <-> goal    ]
             [     pass | fail | part ]
                 fail     |   pass
                   v      v
             [ 4. REPAIR              ]
             [    bounded escalation  ]---> (re-validate -> step 3)
             [    honest STOP         ]
                           |
                           v
             [ 5. HONEST GATE         ]  <- [ 6. GOVERNANCE: approval gates,
             [    verified artifacts  ]      scheduled jobs, durable teams,
             [    + evidence + log    ]      rotating credentials, invariants ]
                           |
                           v
                DELIVERED PRODUCT (real artifacts/files)
\end{lstlisting}

Two observations from the literature justify this layout. \textbf{Graph Harness}
(\cite{graphhs}, arXiv:2604.11378) argues that reAct-style agent loops are
single-ready-unit schedulers
with implicit dependencies, unbounded recovery, and mutable history; lifting control into
an explicit, immutable, validated plan (our \emph{Plan} stage) is precisely the fix it
proposes. \textbf{KAIJU} (\cite{kaiju}, arXiv:2604.02375) demonstrates that separating the
reasoning
layer from the execution layer yields behavioral guarantees that prompting alone cannot.
\textbf{Auton} (\cite{auton}, arXiv:2602.23720) separates a declarative cognitive blueprint
from a
runtime engine with parallel graph execution and dynamic context pruning --- a design
lineage we share.

\subsection{Where This Differs From LangGraph / CrewAI / AutoGen}
The comparison is architectural, not a claim of superiority in every dimension
(Table~\ref{tab:positioning}).

\begin{table}[t!]\centering
\caption{Architectural positioning. \label{tab:positioning}}
\begin{tabular}{@{}lllll@{}}
\toprule
Dimension & LangGraph & CrewAI & AutoGen/MAF & Adam Prism\\
\midrule
Control & explicit graph & role crews & conversation & validated plan\\
Verification & external & external & external & \textbf{native: det. + judge}\\
Repair & manual & retry & retry & \textbf{native bounded + honest}\\
Ideal-for & stateful graphs & rapid prototype & research chat & \textbf{verified artifacts}\\
Local/air-gap & self-host & self-host & self-host & \textbf{designed for it}\\
\bottomrule
\end{tabular}
\end{table}

% ------------------------------------------------------------------
\section{The Verification Model}\label{sec:verify}
\subsection{Verification Is a Layer, Not a Prompt}
Verification consumes the outputs of execution and produces a pass/fail/partial decision
with evidence, ordered by cost and determinism:
\begin{enumerate}
  \item \textbf{Deterministic checks} --- parse, compile, import smoke, and test
        execution of the produced artifact; statistical conformance checks
        (distribution-shape tests, deviation thresholds, duplication detection) for
        data-review tasks. These require no model calls and are byte-identical run to run.
  \item \textbf{Independent spec-conformance judge} --- an LLM instance created separately
        from the builder. It receives the original goal (commands, files, edge cases
        expected) and the produced artifact, and emits a structured pass/fail with
        per-requirement evidence. Because it never inherits the builder's context, it
        cannot ``approve'' a drifted interface on the strength of the builder's confidence.
  \item \textbf{Merge rule} --- a lenient judge cannot clear a deterministic failure.
        Deterministic failures are final; the judge resolves only what rules cannot
        decide.
\end{enumerate}
This ordering mirrors the pattern formalized in FORMALJUDGE (atomic groundable judgments
+ deterministic composition) and AGENTLTL (deterministic, judge-free compliance scores),
and it is cheaper than pure LLM judging: the auxiliary judgment model is an order of
magnitude smaller than the builder.

\subsection{The Honest Repair Loop}
When verification fails, repair is bounded:
\begin{itemize}
  \item \textbf{L0 --- deterministic fixes} --- correct what deterministic rules know how
        to fix directly.
  \item \textbf{L1 --- targeted re-generation} --- re-generate only the defective unit
        with the failure evidence, preserving already-verified work.
  \item \textbf{L2 --- grounding} --- consult external, time-bounded reference material
        (with a caching layer) to supply facts the fix depends on.
  \item \textbf{L3 --- escalate} --- invoke the artifact-repair path; \textbf{stop} if the
        same-signature failure repeats past the ceiling.
\end{itemize}
The loop always terminates in exactly one of two reporting states: \textbf{verified}
(with evidence) or \textbf{not converged} (with an honest log of what failed and why).

\subsection{Deterministic Statistical Validation}
For data-review and compliance scenarios, verification includes statistical screening that
must not vary across runs or environments: profile-shape tests (e.g., digit-distribution
consistency), deviation thresholds against expected baselines, and duplicate and
near-duplicate detection. These checks avoid heavyweight scientific dependencies, so they
run in any environment with no install footprint.

% ------------------------------------------------------------------
\section{Governance and Sovereignty}\label{sec:governance}
A verified artifact is not the same as a \emph{governed} run. Adam Prism's governance
layer sits above the loop:
\begin{itemize}
  \item \textbf{Human-in-the-loop approval gates} on designated steps, so irreversible
        actions are not taken by a stochastic system without consent;
  \item \textbf{Scheduled background jobs} and \textbf{durable agent teams} persisted to
        disk, so work survives restarts;
  \item \textbf{Rotating credential storage} and \textbf{runtime invariant checks}, so
        execution-time assumptions are validated continuously.
\end{itemize}
Sovereignty properties are structural, not claims: local-only execution by default,
air-gap operation tested, and no analytics or telemetry SDKs. The open-source core is
AGPL v3 with a commercial dual license.

% ------------------------------------------------------------------
\section{Evaluation}\label{sec:eval}
\subsection{System-Size Measurements (Reproducible Without an LLM)}
Table~\ref{tab:size} shows the deterministic reliability surface; running the full
regression suite requires no model, so it is usable in CI on any machine.

\begin{table}[h!t]\centering
\caption{Reliability surface. \label{tab:size}}
\begin{tabular}{@{}lr l@{}}
\toprule
Metric & Value & Nature\\
\midrule
Automated tests (pytest) & 1{,}317 & deterministic, no LLM\\
Lint (PEP 8 / pyflakes) & clean & deterministic\\
API routes & 227 & REST + WebSocket + SSE\\
Scenario workflows & 26 (12 product + 14 tree) & declarative YAML\\
LLM calls in regression suite & 0 & ---\\
\bottomrule
\end{tabular}
\end{table}

\subsection{Execution Time and Success Rate}
On the platform's engineering tasks (task-completion runs on a local 12B model), the
planned-and-verified system outperforms free-form prompting (Table~\ref{tab:within});

\begin{table}[h!t]\centering
\caption{Within-platform improvement from plan + verification.
\label{tab:within}}
\begin{tabular}{@{}lll@{}}
\toprule
 & Free-form prompting & Adam Prism (planned + verified)\\
\midrule
Median task completion & 5--10 min & 121--185 s (Table~\ref{tab:registry})\\
Verified success (oracle) & 40--50\% (ad hoc) & 10/10 = 100\% (Table~\ref{tab:registry})\\
Failure handling & implicit retry loops & bounded repair + honest report\\
\bottomrule
\end{tabular}
\end{table}
the gain comes from structured plans (replacing free reasoning) and verification
(replacing hope). For positioning against orchestration frameworks we rely on independent
public 2026 benchmarks rather than our own head-to-head; absolute numbers are not
cross-comparable across hardware and models (Table~\ref{tab:landscape}).

\begin{table}[h!t]\centering
\caption{Landscape survey (sources in \S\ref{sec:related}). \label{tab:landscape}}
\begin{tabular}{@{}lccc@{}}
\toprule
Framework & Token overhead & p50/p95 research latency & Fault recovery\\
\midrule
LangGraph & $\sim$9\% & 14.1 s / 19.8 s & manual\\
CrewAI & $\sim$18\% & 18.4 s / 31.2 s & retry\\
AutoGen & $\sim$12--31\% & 22.7 s / 41.5 s & retry\\
Hermes Agent & $\sim$3--5\% & $\sim$1--2 s (single) & auto-recovery\\
Adam Prism (measured) & 0 (no API) & task-level 121--185 s & \textbf{native bounded repair}\\
\bottomrule
\end{tabular}
\end{table}


\subsection{Quality: What ``Green'' Means}
The central quality claim is about \emph{what passes the gate}:
\begin{itemize}
  \item A product passes only if it is \textbf{functionally exercisable} (imports,
        executes, tests pass) \textbf{and spec-conformant} (an independent judge finds the
        product matches the requested commands/files/edge-cases).
  \item Deterministic failures cannot be waived by any model.
  \item When repair converges, the final gate is re-run on the \emph{actual shipped
        artifact}.
\end{itemize}
Case in point: in engineering runs, syntactically valid but spec-divergent products (the
right file layout, wrong interface; green self-written tests on a drifted contract) were
rejected by the deterministic + independent-judge merge --- a class of failure that a
shallow compile-gate passed silently.

\subsection{Scaling Behavior}
Scaling claims are structural, not load-tested: parallel step dispatch bounds wall-clock
by the critical path rather than the sum of steps; deterministic verification does not
consume tokens, so regression cost grows with work rather than model calls; large
artifacts (disk images, ISOs) are stored as sparse staged builds, re-created from light
config rather than hoarded.

% ------------------------------------------------------------------
\subsection{System-Efficiency Benchmark (measured, local, reproducible)}
Table~\ref{tab:system} reports the measured system-level benchmark (see also
Appendix~\ref{app:bench}): the production two-role pipeline (schema-gated planner,
batched execution, deterministic checks, independent spec review, bounded repair) running
on one RTX~3060 (12 GB) with local Ollama and zero API cost.

\begin{table}[h!t]\centering
\caption{System-efficiency benchmark (artifacts verified green with pytest and
py\_compile). \label{tab:system}}
\begin{tabular}{@{}lr r r@{}}
\toprule
Metric & Mode A (single-role) & Mode B (two-role) & Delta\\
\midrule
Wall-clock per goal (s) & 147--191 & 167--191 & wash\\
Commander-model tokens & 61{,}282 & 36{,}601 & $-40\%$\\
Commander-model calls & 35 & 24 & $-31\%$\\
Executor-channel tokens & 0 & 19{,}620 & $+19{,}620$\\
Total tokens & 61{,}282 & 56{,}221 & $-8\%$\\
Externally verified & both goals & both goals & $=$\\
Financial cost & \$0.00 & \$0.00 & $=$\\
\bottomrule
\end{tabular}
\end{table}

Reading the table honestly:
\begin{enumerate}
  \item \textbf{The split offloads the load-bearing model.} Mode B moves $\sim$35\% of all
        token volume --- and 40\% of leader exposure --- onto the cheap, low-temperature
        executor channel, freeing the commander for planning, judging, and escalation.
  \item \textbf{Wall-clock is not yet a win on a single 12 GB card.} Model swapping in
        and out of VRAM consumes the latency saved; on multi-GPU or co-resident
        deployment, leader offload should translate to wall-time wins.
  \item \textbf{Deterministic confirmation is stable}: every goal produced artifacts that
        pass compilation and tests after exactly one spec-repair round.
  \item \textbf{A verification gap surfaced (our bug, not the model's).} In an early run,
        the generated test file was named \texttt{tests\_word\_count.py} (plural), which
        the default pytest collection pattern (\texttt{test\_*.py}) ignores; the internal
        regression check therefore collected zero tests and reported \emph{pass} while the
        file contained a wrong oracle. Collection semantics are part of verification; our
        external harness now runs every generated test file explicitly, and all final
        artifacts pass. This is a precise, reproducible instance of the verification-gap
        literature (\S\ref{sec:related}).
\end{enumerate}

\subsection{Audit-Registry Benchmark: pass@1 on 10 Independent Deterministic Oracles}
The strongest claim in this paper is not a wall-clock comparison; it is that the
\emph{system} --- not the model --- closes every gap the judge can see. We therefore move
from timing to verification: a second, independent layer in which correctness is decided by
a deterministic oracle rather than by wall time. To measure that we built a
\emph{registry of 10 data-verification scenarios}, each with
its own \emph{deterministic oracle} that checks the produced report against the true
synthetic population (exact-verbatim substring match on the renderer's confirmed-patterns
and facts lines), completely separate from the LLM rubric. Raw drivers and per-task results
are archived in the paper's companion repository (\S B), with the aggregate exposed as
machine-readable JSON (\texttt{audit\_registry}).

\begin{table}[h!t]\centering\small
\caption{Audit-registry benchmark: 10 scenarios, independent deterministic oracles
(one clean run, single process per task).
\label{tab:registry}}
\begin{tabular}{@{}lllll@{}}
\toprule
Task & Wall (s) & Steps & Oracle & VRAM swaps\\
\midrule
Duplicate payment, multi-vendor & 150.8 & 9/9 & 8/8 & 11\\
High-value transactions & 138.6 & 9/9 & 8/8 & 11\\
Missing customer fields & 184.8 & 9/9 & 8/8 & 16\\
Multi-recipient messages & 121.9 & 9/9 & 8/8 & 11\\
Per-team budget overrun & 140.6 & 9/9 & 8/8 & 11\\
Anomalous payroll & 138.3 & 9/9 & 8/8 & 11\\
Ledger in/out-of-balance & 130.0 & 9/9 & 8/8 & 11\\
Empty-category expenses & 125.2 & 9/9 & 8/8 & 11\\
Monthly sales decline & 125.3 & 9/9 & 8/8 & 11\\
Cross-key duplicate pairs & 149.2 & 9/9 & 8/8 & 11\\
\bottomrule
\end{tabular}
\end{table}

Aggregate across \emph{three independent runs} of this fleet (this clean run + two fresh
reruns collected for the controlled study): \textbf{pass@1 = pass@2 = pass@3 = 10/10} ---
30/30 task-runs oracle-green, 80/80 checks in every run, median wall 138.4/127.2/126.3~s,
mean 11.5 VRAM swaps, 0 spec audits (rubric-gated), \$0.00 cost. Every scenario ran the
production channel split (commander gemma4:12b plans/judges, executor qwen2.5-coder:14b
generates), single process per task, with the bounded-repair ladder untouched.

\begin{table}[h!t]\centering\small
\caption{Controlled decomposition on the \emph{same} 10 scenarios, same seeds, same
deterministic oracle. PASS = pipeline success AND oracle green (registry v1 definition).
``System'' rows are independent full-pipeline runs. \label{tab:registry-arms}}
\begin{tabular}{@{}lcccp{5.6cm}@{}}
\toprule
Arm & pass@1 & Oracle & Med.\ wall & Mechanical reading\\
\midrule
System (rep 1, archived) & 10/10 & 80/80 & 138.4 s & full pipeline\\
System (rep 2) & 10/10 & 80/80 & 127.2 s & independent rerun\\
System (rep 3) & 10/10 & 80/80 & 126.3 s & independent rerun\\
Grounding only, no verify/repair & 0/10 & 40/80 & 41.6 s & facts right; contract wrong\\
No grounded classes (ablate) & 7/10 & 78/80 & 146.4 s & 1 honest halt + 1 silent error + 1 halt on correct artifacts\\
Raw model, full data inlined & 0/10 & 60/80 & 12.7 s & prose right; numbers invented\\
\bottomrule
\end{tabular}
\end{table}

Each arm isolates one layer with the identical instrument. The four head-to-head readings:

\begin{itemize}
  \item \textbf{Raw model (free, 0/10; oracle 60/80).} The 12B model receives the full data
        inlined, the same output contract, and the same oracle. It emits the four required
        sections and a valid JSON summary, then misses nearly every numeric fact --- e.g.
        it reports \texttt{total\_rows=794} for a 300-row payments file and a plausible-but-
        wrong count for each injected anomaly class. Med. wall 12.7~s: fast, confident,
        wrong. This is the ceiling the architecture compensates for.
  \item \textbf{Grounding without verification orchestration (noverify, 0/10; oracle 40/80).}
        Data-grounding facts (e.g. \texttt{total\_rows}) now reach the report
        deterministically, but with the attributed-review $\rightarrow$ regeneration loop
        removed the report no longer conforms to the four-section contract: every task
        lands 4/8. The verification loop's measured contribution is deliverable
        \emph{conformance}, not only error-catching.
  \item \textbf{Full pipeline without the grounded anomaly classes (ablate, 7/10; oracle
        78/80).} Restoring the pre-intervention evidence (duplicates / multi-recipient /
        maxima only, no missing-values / overspend / category / growth classes) on the
        identical pipeline reproduces the failure family the fleet once showed: one honest
        halt (missing-field count refused for lack of evidence), one \emph{silent error}
        (empty-category task reports success while the report carries a wrong count, caught
        only by the oracle), and one conservative halt whose persisted artifacts were
        already oracle-correct (8/8). The classes are the point of leverage.
  \item \textbf{Full system (10/10 in all three runs).} With the classes rendered into a
        validator-visible evidence block and named in the regeneration prompt, every
        anomaly family is surfaced and verified verbatim by the independent oracle; the two
        reruns confirm the archived single-run result is not stochastic.
\end{itemize}

Honest deltas not claimed: measurement goals that require external LLM availability or a
third model family; the two registries measure \emph{system reproducibility and layer
attribution} across two goal families, not general agent ability. The ``5 honest halts''
anecdote recorded earlier in this paper was the natural experiment behind the ablate arm:
same tasks, same seeds, the single changed variable is whether the anomaly classes are
rendered into the evidence block.

\subsection{Second Goal Family: Executable-Oracle Code Generation}
The audit registry above measures one goal family (structured CSV data audits) with oracles
that \emph{read the produced report's text}. To break that limitation --- and to show the
verification loop is not specialized to text reports --- we built a
second family of 5 code-generation scenarios whose oracles are
\emph{executable}: the produced \texttt{module.py} is imported in a subprocess and run
against hidden deterministic cases whose expected outputs are computed from a reference
implementation --- no LLM, no rubric, only \texttt{py\_compile} plus real execution.

\begin{center}
\begin{tabular}{@{}lcclc@{}}
\toprule
Task & Function & Wall med (s) & Oracle & pass@3\\
\midrule
mergesort & \texttt{sort\_nums} & 66.2 & 5/5 & 3/3\\
dedupe\_pairs & \texttt{dedupe\_pairs} & 63.8 & 5/5 & 3/3\\
payroll\_net & \texttt{net\_pay} & 63.8 & 5/5 & 3/3\\
topk\_words & \texttt{top\_k\_words} & 62.7 & 5/5 & 3/3\\
budget\_overspend & \texttt{find\_overspending} & 60.5 & 5/5 & 3/3\\
\bottomrule
\end{tabular}
\end{center}

Each task receives its exact requirement spec (the code-family equivalent of the gold facts
in \S5.6) and follows the same 8-step scenario: plan, review-plan, list, write --- via the
executor channel \texttt{qwen2.5-coder:14b} --- review code (sandboxed analysis JSON),
render a deterministic report grounded in the produced files, attributed review, global
verify. The review gate targets the code-review product (function, signature, edge cases,
test count) rather than report prose, and report rendering is deterministic (grounding
evidence block + artifacts) with LLM re-writes disabled, which removes fence-wrapping
noise from the judged output. Aggregation: \textbf{pass@1 = pass@2 = pass@3 = 5/5}
(\textbf{15/15} task-runs, \textbf{75/75} hidden executable checks), median wall 60--66 s,
\$0.00 cost, all single-process and single-shot. The internal gate (\texttt{system\_success})
and the external executable oracle agree on every run. An early spec-less pilot (payroll
tax brackets invented from memory) failed the oracle 0/9 and was honestly rejected,
reproducing the requirement-grounding finding of \S5.6 in the code family.

\section{Limitations and Honest Assessment}\label{sec:limits}
State plainly:
\begin{enumerate}
  \item \textbf{Raw model skill is low on open service-desk dialogue.} We wired
        $\tau$-bench~\cite{tau} (June-2024 tasks, airline + retail) to run fully offline
        through the
        OpenAI-compatible endpoint of our local Ollama service ($\$0.00$). Tool-calling
        strategy, gemma4:12b: airline task 0 $\rightarrow$ reward 0.0 (two correct tool
        calls, then eight degenerate empty-call turns); retail 0/4 completed in $\sim$14
        minutes; ReAct stalls identically. This is not Adam Prism's design target:
        $\tau$-bench measures open dialogue, whereas Adam Prism is verification-driven
        artifact production. Public-benchmark participation requires either a stronger
        local tool-calling model or an artifact-aligned benchmark (\S\ref{sec:intro}).
        Complementing this \emph{model} probe, the \emph{system}-level measures succeed:
        Table~\ref{tab:system} reports both artifact goals verified green in $\sim$150--190~s
        each, and the audit registry reaches single-shot pass@1 = 10/10 with independent
        deterministic oracles (Table~\ref{tab:registry}) --- all on a single consumer GPU
        at \$0, and each surfaced real internal defects (the pytest collection pattern; the
        5/10 honest-halt grounding gap) that we fixed and report transparently. The honest
        reading is not ``the model is weak'' but ``open service-desk dialogue is out of
        scope for a 12B local model; verified artifact production is where the architecture
        converts that ceiling into a reliable floor.'
  \item \textbf{Model capability sets a ceiling.} A 12B local model cannot match
        frontier-model reasoning on open-ended research. The claim is that structured
        plan+verify beats free reasoning \emph{for artifact-producing goals}.
  \item \textbf{Independent judge $\in$ same model family.} The judge is instantiated
        separately but on the same underlying model line; true independence would use a
        different model family or a formal verifier.
  \item \textbf{Storage pressure.} Producing large artifacts (Linux images) fills local
        disks quickly; sparse/staged flows mitigate but do not eliminate this.
  \item \textbf{No longitudinal reliability study.} ``Acts'' (one tool call) vs.\ verified
        ``products'' (materialized, tested artifacts) remain under-measured.
\end{enumerate}

% ------------------------------------------------------------------
\section{Related Work}\label{sec:related}
\begin{itemize}
  \item \textbf{Multi-agent orchestration:} LangGraph (LangChain), CrewAI, AutoGen /
        Microsoft Agent Framework, Hermes Agent (Nous Research). Independent 2026
        benchmarks~\cite{bench2026}: agent-harness.ai, tacavar.com 2026 comparison, JATIR
        Vol 2(6), 107-task
        bakeoffs. We cite their published metrics, not our own runs.
  \item \textbf{Verification and control separation:} FORMALJUDGE~\cite{formajudge};
        Sovereign Agentic Loops~\cite{sovereign}; Graph Harness~\cite{graphhs};
        AgentLTL~\cite{agentltl}; Verified Multi-Agent Orchestration~\cite{vmao};
        AgentSPEX~\cite{agentspex}; Auton~\cite{auton}.
  \item \textbf{Evaluation methodology:} General AgentBench~\cite{gentbench} ---
        verification gap; ToolBench-X~\cite{toolbenchx} --- diagnosis over compute;
        AGENCYBENCH~\cite{agencyb} (ACL 2026) --- long-horizon rubric evaluation;
        $\tau$-bench~\cite{tau} and $\tau^2$-bench~\cite{tau2} --- tool-agent-user dialogue
        with database-state evaluation.
\end{itemize}

% ------------------------------------------------------------------
\section{Conclusion and Open Problems}\label{sec:conclusion}
We presented Adam Prism, a sovereignty-first agent that separates planning, execution,
verification, repair, and governance; grounds evaluation in deterministic checks plus an
independent judge; and reports honest pass/fail instead of plausible completion. On a
local 12B model it sustains 1{,}317 deterministic tests and 26 scenario workflows, and on
\textbf{two oracle-verified registries} --- 10 CSV-verification scenarios (single-shot
pass@1 = 10/10) and 5 code-generation scenarios (15/15 task-runs, 75/75 hidden executable
checks) --- the verification loop closes \emph{without} an LLM judging the outcome. It
remains air-gap capable and free of per-token cost, and its failure taxonomy is reported
rather than hidden.

Open problems we consider most important:
\begin{enumerate}
  \item Public-benchmark participation (SWE-bench-style, tau-bench, AgentGym2) with
        disclosed hardware/model/temperature.
  \item Cross-family judges --- a formal verifier or a different-family judge to break the
        shared-bias caveat.
  \item Procedural-compliance harnesses (AgentLTL-style) that score the \emph{process} of
        agent execution, not only the output.
  \item Reproducible artifact pipelines as a first-class open-source deliverable.
\end{enumerate}

The through-line that unifies these results is architectural, not algorithmic: correctness
is earned by structure --- a gated plan, a separated verifier, an honest stop --- rather
than by hoping a larger model will ``behave.'' Given a sovereign-friendly model (12--26B)
on a single GPU, Adam Prism converts an open-ended agent into a \emph{deterministic
production channel}: same hardware, same model family, but verification whose credit
derives from deterministic checks and independent oracles. We believe the principle
generalizes to any artifact-producing goal family, and we put the data --- and the failures
--- beside the claim.

% ------------------------------------------------------------------
\begin{appendix}
\section{Publication Checklist}

\section{Block Diagram Legend (no code, no internal identifiers)}
Blue boxes = the verification-driven loop (Section 2.2). Green box = governance plane
(Section 4). Arrows denote artifact flow, not control coupling; the judge consumes only
the goal and the artifact, never the builder's context.

\section{Reproducible Offline \texorpdfstring{$\tau$}{tau}-bench Harness (measured, honest)}
\begin{enumerate}
  \item \texttt{git clone https://github.com/sierra-research/tau-bench}
  \item \texttt{pip install -e .} (requires Python $\ge$ 3.11; latest litellm needs
        \texttt{typing.NotRequired})
  \item Harness patches (local only): litellm returns \texttt{response\_cost=None} for
        local endpoints, so the agent/user cost bookkeeping coerce \texttt{None
        $\rightarrow$ 0}.
  \item Point litellm at the local endpoint:
        \texttt{OPENAI\_API\_KEY=ollama}\\[-2pt]
        \texttt{OPENAI\_API\_BASE=http://localhost:11434/v1}
\end{enumerate}
Measured on RTX~3060 (12 GB), 31 GB RAM, Ollama:
\begin{center}
\begin{tabular}{@{}llll@{}}
\toprule
Config & Task set & Completed & Reward\\
\midrule
tool-calling, gemma4:12b & airline 0 & 1/1 & 0.0\\
tool-calling, gemma4:12b & retail 0--3 & 0/4 & ---\\
react, gemma4:12b & airline 0--2 & 0/3 & ---\\
\bottomrule
\end{tabular}
\end{center}

\section[System-Efficiency Benchmark (reproduce)]{System-Efficiency Benchmark (reproduce)%
\label{app:bench}}
Aggregate results in
\texttt{WHITE\_PAPER\_BENCHMARK\_RESULTS.json}.
The Table~\ref{tab:system} numbers were produced by driving the \emph{production} pipeline
with per-channel instrumentation on the HTTP layer; token usage is read from Ollama's own
\emph{prompt\_eval\_count}/\emph{eval\_count}; wall-time and call counts are recorded per
channel. To reproduce:
\begin{enumerate}
  \item Set \texttt{ADAM\_OLLAMA\_MODEL=gemma4:12b} and
        \texttt{ADAM\_EXECUTOR\_MODEL=qwen2.5-coder:14b} (Mode B) or empty (Mode A).
  \item Use two fixed goals (word-frequency module + CLI Fibonacci tool).
  \item External confirmation runs every generated \texttt{*test*} file explicitly
        with pytest --- explicit collection avoids the \texttt{test\_*.py}
        default-pattern gap of \S\ref{sec:eval} --- plus \texttt{py\_compile}.
\end{enumerate}
Host: RTX 3060 12 GB, Ollama, server-side model load.
\end{appendix}

\begin{thebibliography}{99}
\bibitem{formajudge} FORMALJUDGE.\  arXiv:2602.11136, 2026.
\bibitem{agentltl} AgentLTL.\  arXiv:2607.02599, 2026.
\bibitem{vmao} Verified Multi-Agent Orchestration.\  arXiv:2603.11445, 2026.
\bibitem{toolbenchx} ToolBench-X.\  arXiv:2606.25819, 2026.
\bibitem{gentbench} General AgentBench.\  arXiv:2602.18998, 2026.
\bibitem{graphhs} Graph Harness.\  arXiv:2604.11378, 2026.
\bibitem{kaiju} KAIJU.\  arXiv:2604.02375, 2026.
\bibitem{auton} Auton Agentic AI Framework.\  arXiv:2602.23720, 2026.
\bibitem{sovereign} Sovereign Agentic Loops.\  arXiv:2604.22136, 2026.
\bibitem{agentspex} AgentSPEX.\  arXiv:2604.13346, 2026.
\bibitem{agencyb} AGENCYBENCH.\  ACL 2026, industry track.
\bibitem{bench2026} agent-harness.ai benchmark; tacavar.com 2026; JATIR Vol 2(6); 107-task
  bakeoffs (independent 2026 measurements).
\bibitem{tau} S.~Yao, N.~Shinn, P.~Razavi, K.~Narasimhan.\  $\tau$-bench.\  arXiv:2406.12045,
  2024.
\bibitem{tau2} V.~Barres, H.~Dong, S.~Ray, X.~Si, K.~Narasimhan.\  $\tau^2$-bench.\
  arXiv:2506.07982, 2025.
\end{thebibliography}

\end{document}